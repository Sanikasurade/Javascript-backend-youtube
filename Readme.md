#Chai aur backend series
steps
1) npm init
2) create folders public and temp and keep the .gitkeep file in temp folder
3) noe create the .gitignore => gitignore generator(use to mrkandreev.name) for generating the .gitignore file
4) npm i -D nodemon : dependency to restart the server 